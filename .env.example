# Copy to .env and fill in your key (this file is committed; .env is NOT).
# Get a key: Google Cloud Console → APIs & Services → enable "YouTube Data API v3" → create API key.
# Recommended: restrict the key to YouTube Data API v3 + IP/referrer.
YOUTUBE_API_KEY=your_youtube_data_api_v3_key_here
